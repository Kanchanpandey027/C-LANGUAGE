#include <stdio.h>
#include <stdlib.h>

int main()
{
    int m;
    printf("enter data:");
    scanf("%d",&m);
    if(m>0)
        printf("the value of n=1");
     else if(m=0)
        printf("the value of n=0");
    else
         printf("the value of n=-1");
    return 0;
}
